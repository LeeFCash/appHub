https://adoptium.net/temurin/releases/?os=any&arch=any&version=21&mode=filter # use the website to download a to my understanding prcompiled java dev kit to use java without the user needing to have java installed 

https://adoptium.net/temurin/releases # website to install java runtime for exe

tar -xvf # the command to extract the java runtime files

|---|# for arch linux exe
pyinstaller --onedir \
  --add-data "jarFiles:jarFiles" \
  --add-data "templates:templates" \
  --add-data "static:static" \
  app.py
# or
  pyinstaller --onefile \
  --add-data "jarFiles:jarFiles" \
  --add-data "templates:templates" \
  --add-data "static:static" \
  app.py


  |---| # windows exe
  pyinstaller --onefile ^
  --add-data "jarFiles;jarFiles" ^
  --add-data "templates;templates" ^
  --add-data "static;static" ^
  app.py
|---|



if doing it by using more then one exe then 
pyinstaller --onedir \
  --add-data "templates:templates" \
  --add-data "static:static" \
  --add-data "exe:exe" \
  app.py

windows 11 do 
pyinstaller --onedir ^
  --add-data "templates;templates" ^
  --add-data "static;static" ^
  --add-data "exe;exe" ^
  app.py

folder with executable and other files needed to run but run the one executable then it works no install needed.
